import { writable } from 'svelte/store';

export const cytosis = writable(undefined);
export const makers = writable(undefined);
export const dropoff = writable(undefined);
export const content = writable(undefined);